<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".InkDropApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InkDrop"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Main Settings Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.InkDrop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Share Receiver Activity -->
        <activity
            android:name=".ShareReceiverActivity"
            android:exported="true"
            android:theme="@style/Theme.InkDrop.Transparent"
            android:excludeFromRecents="true"
            android:taskAffinity="">

            <!-- Twitter/X URL share intents -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Direct URL handling -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="twitter.com" android:pathPattern="/.*" />
                <data android:scheme="https" android:host="x.com" android:pathPattern="/.*" />
                <data android:scheme="https" android:host="www.twitter.com" android:pathPattern="/.*" />
                <data android:scheme="https" android:host="www.x.com" android:pathPattern="/.*" />
                <data android:scheme="https" android:host="mobile.twitter.com" android:pathPattern="/.*" />
                <data android:scheme="https" android:host="mobile.x.com" android:pathPattern="/.*" />
            </intent-filter>
        </activity>

    </application>

</manifest>
